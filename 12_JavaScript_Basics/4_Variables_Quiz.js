// what's bankBalance after the following code runs

let bankBalance = 100;
bankBalance /= 2;
bankBalance += 10;

// answer: 60


// what's score after the code runs
let score = 10;
score + 4;

// answer: score is 10 because score was never update (score = score + 4)


// what's the value of age after the code runs
const age = 64;
age++;

// answer: age is 64 because constants can't be updated and const variables cannot be reassigned