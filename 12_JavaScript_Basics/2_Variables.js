// jars that store values
// let someName = value;


let numHens = 5;
let numRoosters = 1;

console.log(numHens + numRoosters);

// snapshot in time. If numHens is updated later, totalChickens's value will not change
// Not intrinsically linked
let totalChickens = numHens + numRoosters;

// Define a variable called myLuckyNumber and set it equal to 37
let myLuckyNumber = 37;
let octopusLimbs = 8;


// updating variables
let numLives = 9;

// increment
numLives++;
numLives += 4;
numLives = numLives + 1;

// decrement
numLives--;
numLives -= 3;
numLives = numLives - 2;

console.log(numLives);