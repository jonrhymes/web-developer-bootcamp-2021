// function expressions
// storing a function in a variable

const square = function (num) {
    return num + num;
}

console.log(square(4));

// functions are objects
// function expressions do not have a name, but the variable does